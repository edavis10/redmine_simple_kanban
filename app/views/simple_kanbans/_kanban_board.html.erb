<div id="kanban-board">
  <div class="swimlane" id="backlog-swimlane">
    <%= content_tag(:h3, l(:simple_kanban_label_backlog_swimlane)) %>
    <div>
      <% @backlog_issues.each do |project, issues| %>
        <%= content_tag(:h4, link_to_project(project)) %>
        <%= render :partial => 'issue', :collection => issues %>
      <% end %>
    </div>
  </div>

  <div class="swimlane" id="next-swimlane">
    <%= content_tag(:h3, l(:simple_kanban_label_next_swimlane)) %>
    <div>
      <% @next_issues.each do |project, issues| %>
        <%= content_tag(:h4, link_to_project(project)) %>
        <%= render :partial => 'issue', :collection => issues %>
      <% end %>
    </div>
  </div>

  <div class="swimlane" id="in-progress-swimlane">
    <%= content_tag(:h3, l(:simple_kanban_label_in_progress_swimlane)) %>
    <div>
      <% @in_progress_issues.each do |project, issues| %>
        <%= content_tag(:h4, link_to_project(project)) %>
        <%= render :partial => 'issue', :collection => issues %>
      <% end %>
    </div>
  </div>

  <div class="swimlane" id="acceptance-swimlane">
    <%= content_tag(:h3, l(:simple_kanban_label_acceptance_swimlane)) %>
    <div>
      <% @acceptance_issues.each do |project, issues| %>
        <%= content_tag(:h4, link_to_project(project)) %>
        <%= render :partial => 'issue', :collection => issues %>
      <% end %>
    </div>
  </div>

  <div class="swimlane" id="done-swimlane">
    <%= content_tag(:h3, l(:simple_kanban_label_done_swimlane)) %>
    <div>
      <% @done_issues.each do |project, issues| %>
        <%= content_tag(:h4, link_to_project(project)) %>
        <%= render :partial => 'issue', :collection => issues %>
      <% end %>
    </div>
  </div>

</div>

<div style="clear:both;"></div>
